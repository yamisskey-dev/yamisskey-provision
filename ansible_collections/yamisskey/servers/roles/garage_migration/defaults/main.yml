---
# Garage migration defaults
garage_migration_bucket_name: "{{ minio_bucket_name_for_misskey | default('files') }}"
garage_migration_rclone_config_dir: /root/.config/rclone
garage_migration_transfers: 8
garage_migration_checkers: 16
garage_migration_chunk_size: 64M
