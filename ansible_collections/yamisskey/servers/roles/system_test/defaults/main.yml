---
# テスト設定変数
system_test_required_tools:
  - ansible
  - docker
  - git

system_test_optional_tools:
  - tailscale
  - cloudflared
  - warp-cli

# サービスエンドポイント設定
system_test_service_endpoints:
  - { port: "{{ host_services[inventory_hostname].garage_api | default(3900) }}", path: "/health", name: "Garage", required: false }
  - { port: "{{ host_services[inventory_hostname].misskey | default(3000) }}", path: "/api/ping", name: "Misskey", required: false }
  - { port: "{{ host_services[inventory_hostname].prometheus | default(9090) }}", path: "/-/healthy", name: "Prometheus", required: false }
  - { port: "{{ host_services[inventory_hostname].grafana | default(3000) }}", path: "/api/health", name: "Grafana", required: false }

# テスト結果設定
system_test_test_results: []
system_test_passed_tests: 0
system_test_total_tests: 6

# テスト実行制御
system_test_run_infrastructure_tests: true
system_test_run_migration_tests: true
system_test_run_service_tests: true
system_test_fail_on_critical_errors: true
