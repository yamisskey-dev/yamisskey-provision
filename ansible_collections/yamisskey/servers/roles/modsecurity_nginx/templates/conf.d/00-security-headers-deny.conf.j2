# Security headers configuration for services that deny framing
# Use this for: MinIO, Synapse

# Include common security headers (HSTS, X-Content-Type-Options, etc.)
include /etc/nginx/conf.d/00-security-headers.conf;

# X-Frame-Options (DENY to prevent any framing - more secure for APIs)
add_header X-Frame-Options "DENY" always;
